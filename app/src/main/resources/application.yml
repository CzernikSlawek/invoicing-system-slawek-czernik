server:
  port: 6666

invoicing-system:
  database:
    type: file
    dbPath: invoices.json

spring:
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
      profiles:
        active: memory
      flyway:
        enabled: false